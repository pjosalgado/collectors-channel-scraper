# collectors-channel-scraper
Built with ❤️ using [Scrapy](https://scrapy.org)


## Run locally
Edit and execute ``env-example.sh`` to configure the necessary environment variables and run the application using ``run.sh``.
